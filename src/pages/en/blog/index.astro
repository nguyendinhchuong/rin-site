---
import Layout from '@/layouts/Layout.astro';
import BlogCard from '@/components/BlogCard';
import { sanityClient, urlFor } from '@/lib/sanity';
import { allPostsQuery } from '@/lib/queries';
import { generateBreadcrumbSchema } from '@/lib/seo';

export const prerender = false;

const locale = 'en';

const posts = await sanityClient.fetch(allPostsQuery, { language: locale });

const siteUrl = Astro.site?.toString() || 'https://vinhson.com.vn';

const breadcrumbSchema = generateBreadcrumbSchema([
  { name: 'Home', url: `${siteUrl}/en` },
  { name: 'Blog', url: `${siteUrl}/en/blog` },
]);
---

<Layout
  title="Blog - Vinh Son"
  description="Stay updated with latest news and trends"
  schema={breadcrumbSchema}
>
  <div class="container mx-auto px-4 py-8">
    <div class="mb-8">
      <h1 class="mb-2 text-4xl font-bold">Blog</h1>
      <p class="text-muted-foreground">Stay updated with latest news and trends</p>
    </div>

    {posts && posts.length > 0 ? (
      <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
        {posts.map((post: any) => (
          <BlogCard client:load post={post} urlFor={urlFor} locale={locale} />
        ))}
      </div>
    ) : (
      <div class="py-16 text-center">
        <p class="text-lg text-muted-foreground">No posts available.</p>
      </div>
    )}
  </div>
</Layout>

